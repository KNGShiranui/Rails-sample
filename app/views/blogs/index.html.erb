<h1>ブログ一覧</h1>
<%
=begin #注意点として、beginとendの=（イコール）は左端に書く必要がある。
%>　begin からendまでがコメントアウトされる。
ターミナル上では、すべてのレコードが表示されましたが、アプリ上ではBlog.allの結果を出力しても、ただBlog::ActiveRecord::Relation...と出力されるだけです。
これは、<%= @blogs %>では、ただ、「@blogsが配列である」という情報しかhtmlに出力されていないためです。本来であれば、配列の中身1つ1つの詳細が見たいのですが、
<%= @blogs %>の記述では、@blogsの配列の中身を展開してはくれません。
<%  
=end
%>

<p><%= notice %></p>

<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr> 
<% @blogs.each do |blog| %>
<%
=begin
%>
ターミナル上では、すべてのレコードが表示されましたが、アプリ上ではBlog.allの結果を出力しても、ただBlog::ActiveRecord::Relation...と出力されるだけです。
これは、<%= @blogs %>では、ただ、「@blogsが配列である」という情報しかhtmlに出力されていないためです。本来であれば、配列の中身1つ1つの詳細が見たいのですが、
<%= @blogs %>の記述では、@blogsの配列の中身を展開してはくれません。<% @blogs.each do |blog| %>として配列の中身を個別に表示する。
<%
=end
%>
  <tr>
    <td><%= blog.title %></td>
    <td><%= blog.description %></td>
<%
=begin
%>
<%= link_to '詳細を確認する', blog_path %>これだとエラーになる。
<%
=end
%>
<td><%= link_to '詳細を確認する', blog_path(blog.id) %></td> #こういう風にIDを渡した状態でないとダメ
<td><%= link_to 'ブログを編集する', edit_blog_path(blog.id) %></td>
<td><%= link_to 'ブログを削除する', blog_path(blog.id), method: :delete ,data: { confirm: '本当に削除していいですか？' } %></td>
  </tr>
<% end %>
</table>

<%= link_to '新しくブログを投稿する', new_blog_path %>



<p>Find me in app/views/blogs/index.html.erb</p>


